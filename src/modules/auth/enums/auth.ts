export enum EAuthRoles {
  USER = 'user',
  COORDINATOR = 'coordinator',
  ADMIN = 'admin',
  INITIATIVE_ADMIN = 'initiative-administrator'
}

export enum Status {
  WAITING = 'waiting',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
